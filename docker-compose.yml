services:
  service-pg:
    image: postgres:lastest
    ports:
      -"5432:5432"
    environment:
      - POSTGRES_USER=postgresuser
      - POSTGRES_PASSWORD=postgrespwd
      - POSTGRES_DB=postgresdb